The input files contains tree parts.
1. Clusters, it should satisfy the clusterSchema.json.
2. Nodes: it should satisfy the nodeSchema.json. Node json file can have more than one.
3. Hashtags: it is a csv file, the first line is "hashtag", the rest of them are hashtags, one hashtag for each line.